# Developer Workflow Guide

## 🚀 Автоматизированная система управления задачами

Наш проект использует автоматизированную систему GitHub Actions для управления issues и pull requests. Эта система автоматически связывает PR с issues и управляет их статусами.

## 📋 Как работать с системой

### 1. Создание Issue

1. **Создайте issue** в проекте в статусе Todo
2. **Issue автоматически получит префикс** `NOCOUNTRY-<номер>` в заголовке спустя несколько секунд, нужно подождать

### 2. Создание Pull Request

1. **Создайте ветку** с названием в виде префикса issue: (**Ахтунг! без этого все остадьное бессмысленно**)
   ```bash
   git checkout -b NOCOUNTRY-123
   ```

2. **Система автоматически**:
   - Добавит префикс ветки к заголовку PR
   - Свяжет PR с соответствующей issue
   - Добавит комментарии в issue и PR

### 3. Автоматические действия

#### При создании PR:
- ✅ **Префикс ветки** добавляется к заголовку PR
- ✅ **Связывание** PR с issue по префиксу `NOCOUNTRY-<номер>`
- ✅ **Комментарии** добавляются в issue и PR

#### При закрытии PR:
- ✅ **Issue автоматически закрывается** с комментарием о причине

## 🔧 Правила именования

### Ветки
```bash
# ✅ Правильно
NOCOUNTRY-14

# ❌ Неправильно
feature-name
fix-bug
auth-issue
```