name: Link PR to Issue by NOCOUNTRY key

on:
  pull_request:
    types: [opened, edited, reopened, synchronize]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  prefix-title:
    runs-on: ubuntu-latest
    steps:
      - name: Prefix PR title with branch name
        uses: actions/github-script@v7
        with:
          script: |
            const pr = context.payload.pull_request;

            // Skip forks (token will not have write permissions)
            if (pr.head.repo.full_name !== pr.base.repo.full_name) {
              console.log("⚠️ PR is from a fork — skipping title prefixing");
              return;
            }

            const { owner, repo } = context.repo;
            const branchName = pr.head.ref;
            const prefix = `${branchName}: `;

            if ((pr.title || "").startsWith(prefix)) {
              console.log("ℹ️ Title already prefixed with branch name");
              return;
            }

            const updatedTitle = `${prefix}${pr.title}`;
            await github.rest.pulls.update({
              owner,
              repo,
              pull_number: pr.number,
              title: updatedTitle
            });
            console.log(`✅ Prefixed PR title with branch name: "${updatedTitle}"`);

  link:
    runs-on: ubuntu-latest
    needs: prefix-title
    steps:
      - name: Link PR to issue
        uses: actions/github-script@v7
        with:
          script: |
            const pr = context.payload.pull_request;

            // Пропускаем PR из fork — нет прав писать
            if (pr.head.repo.full_name !== pr.base.repo.full_name) {
              console.log("⚠️ PR is from a fork — skipping linking");
              return;
            }

            // Ищем префикс вида NOCOUNTRY-123 в названии PR
            const match = pr.title.match(/NOCOUNTRY-(\d+)/i);
            if (!match) {
              console.log("❌ No NOCOUNTRY-<number> found in PR title");
              return;
            }

            const issueNumber = match[1];
            const issueKey = `NOCOUNTRY-${issueNumber}`;

            // Ищем issue по префиксу в названии через Search API (учитывает и открытые, и закрытые)
            const q = `repo:${context.repo.owner}/${context.repo.repo} is:issue in:title "${issueKey}"`;
            const search = await github.rest.search.issuesAndPullRequests({ q, per_page: 50 });
            const linkedIssue = search.data.items.find(i => i.title.startsWith(issueKey));
            if (!linkedIssue) {
              console.log(`⚠️ Issue with prefix ${issueKey} not found via search`);
              return;
            }

            // Проверим, нет ли уже такого комментария, чтобы не дублировать на synchronize
            const existingComments = await github.paginate(
              github.rest.issues.listComments,
              {
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: linkedIssue.number,
                per_page: 100
              }
            );
            const hasComment = existingComments.some(c => (c.body || "").includes(`#${pr.number}`));
            if (!hasComment) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: linkedIssue.number,
                body: `🔗 **Связанный PR:** [#${pr.number} - ${pr.title}](${pr.html_url})`
              });
            } else {
              console.log(`ℹ️ Comment already exists for PR #${pr.number} on issue #${linkedIssue.number}`);
            }

            // Обновим описание PR: заменим существующую строку, чтобы не плодить дубликаты
            const markerRegex = /\*\*Связанная issue:\*\*\s*#\d+/i;
            const currentBody = pr.body || "";
            const desiredLine = `**Связанная issue:** #${linkedIssue.number}`;
            let updatedBody;
            if (markerRegex.test(currentBody)) {
              updatedBody = currentBody.replace(markerRegex, desiredLine);
            } else {
              updatedBody = `${currentBody}\n\n${desiredLine}`.trim();
            }

            if (updatedBody !== currentBody) {
              await github.rest.pulls.update({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: pr.number,
                body: updatedBody
              });
            } else {
              console.log("ℹ️ PR body already contains the correct linked issue line");
            }

            console.log(`✅ Linked PR #${pr.number} → Issue #${linkedIssue.number} (${issueKey})`);
